---
title: "Get Template"
api: "GET /api/templates/{id}"
---

One notification template, including its body and the variables it interpolates.

Requires an admin bearer token.

## Path parameters

<ParamField path="id" type="string" required />

## Response

<ResponseField name="id" type="string" />

<ResponseField name="event_type" type="string" />

<ResponseField name="channel" type="string">
  One of `email`, `sms`, `in_app`.
</ResponseField>

<ResponseField name="locale" type="string" default="en" />

<ResponseField name="subject" type="string">
  Email only.
</ResponseField>

<ResponseField name="body" type="string" />

<ResponseField name="is_active" type="boolean" />

## Errors

| Status | Code | When |
|--------|------|------|
| 401 | `unauthorized` | Missing or invalid admin token. |
| 404 | `not_found` | No template with that id. |

<CodeGroup>

```bash curl
curl "https://auth.mgpass.net/api/templates/usr_abc123" \
  
```

```typescript TypeScript
const res = await fetch("https://auth.mgpass.net/api/templates/usr_abc123", {
  headers: {  },
});
const data = await res.json();
```

</CodeGroup>
