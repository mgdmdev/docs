---
title: "List Benefits"
api: "GET /api/rewards/benefits"
---

Every tier benefit. A benefit is what a tier entitles a user to beyond points — a discount, free delivery, early access.

Requires an admin bearer token.

## Response

An array of items.

<ResponseField name="id" type="string" />

<ResponseField name="tier" type="string" />

<ResponseField name="title" type="string" />

<ResponseField name="description" type="string" />

<ResponseField name="icon" type="string" />

<ResponseField name="is_active" type="boolean" />

## Errors

| Status | Code | When |
|--------|------|------|
| 401 | `unauthorized` | Missing or invalid admin token. |

<CodeGroup>

```bash curl
curl "https://auth.mgpass.net/api/rewards/benefits" \
  
```

```typescript TypeScript
const res = await fetch("https://auth.mgpass.net/api/rewards/benefits", {
  headers: {  },
});
const data = await res.json();
```

</CodeGroup>
