---
title: "Create Benefit"
api: "POST /api/rewards/benefits"
---

Defines a tier benefit.

Requires an admin bearer token.

## Body

<ParamField body="id" type="string" />

<ParamField body="tier" type="string" />

<ParamField body="title" type="string" />

<ParamField body="description" type="string" />

<ParamField body="icon" type="string" />

<ParamField body="is_active" type="boolean" />

## Response

<ResponseField name="id" type="string" />

<ResponseField name="tier" type="string" />

<ResponseField name="title" type="string" />

<ResponseField name="description" type="string" />

<ResponseField name="icon" type="string" />

<ResponseField name="is_active" type="boolean" />

## Errors

| Status | Code | When |
|--------|------|------|
| 400 | `invalid_request` | `name` or `benefit_type` is missing. |
| 401 | `unauthorized` | Missing or invalid admin token. |

<CodeGroup>

```bash curl
curl -X POST "https://auth.mgpass.net/api/rewards/benefits" \
   \
  -H "Content-Type: application/json" \
  -d '{
  "id": "<string>",
  "tier": "<string>",
  "title": "<string>",
  "description": "<string>",
  "icon": "<string>",
  "is_active": true
}'
```

```typescript TypeScript
const res = await fetch("https://auth.mgpass.net/api/rewards/benefits", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({
    "id": "<string>",
    "tier": "<string>",
    "title": "<string>",
    "description": "<string>",
    "icon": "<string>",
    "is_active": true
  }),
});
const data = await res.json();
```

</CodeGroup>
