---
title: "Get Benefit"
api: "GET /api/rewards/benefits/{id}"
---

One tier benefit.

Requires an admin bearer token.

## Path parameters

<ParamField path="id" type="string" required />

## Response

<ResponseField name="id" type="string" />

<ResponseField name="tier" type="string" />

<ResponseField name="title" type="string" />

<ResponseField name="description" type="string" />

<ResponseField name="icon" type="string" />

<ResponseField name="is_active" type="boolean" />

## Errors

| Status | Code | When |
|--------|------|------|
| 401 | `unauthorized` | Missing or invalid admin token. |
| 404 | `not_found` | No benefit with that id. |

<CodeGroup>

```bash curl
curl "https://auth.mgpass.net/api/rewards/benefits/usr_abc123" \
  
```

```typescript TypeScript
const res = await fetch("https://auth.mgpass.net/api/rewards/benefits/usr_abc123", {
  headers: {  },
});
const data = await res.json();
```

</CodeGroup>
