---
title: "List Roles"
api: "GET /api/roles"
---

Every role defined on the platform, with the scopes each one grants.

Requires an admin bearer token.

## Response

An array of items.

<ResponseField name="id" type="string" />

<ResponseField name="name" type="string" />

<ResponseField name="description" type="string" />

<ResponseField name="type" type="string">
  `user` roles are assigned to people; `m2m` roles to machine clients. One of `user`, `m2m`.
</ResponseField>

<ResponseField name="is_default" type="boolean">
  True when the role is granted automatically to new users.
</ResponseField>

<ResponseField name="scopes" type="array" />

<ResponseField name="created_at" type="integer">
  Unix epoch seconds.
</ResponseField>

## Errors

| Status | Code | When |
|--------|------|------|
| 401 | `unauthorized` | Missing or invalid admin token. |

<CodeGroup>

```bash curl
curl "https://auth.mgpass.net/api/roles" \
  
```

```typescript TypeScript
const res = await fetch("https://auth.mgpass.net/api/roles", {
  headers: {  },
});
const data = await res.json();
```

</CodeGroup>
